import React from "react";
import styles from "./styles/TweetButton.module.css";

export default function TweetButton() {
  return (
    <button onClick={() => {}} className={styles.button}>
      tweet
    </button>
  );
}
